# ── Anthropic API ──────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ── Telegram Bot ───────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-bot-token-here

# 【可選】備用靜態 Chat ID，若未設定則完全由訂閱者系統管理
# 使用 bot.py 時，訂閱者由 /subscribe 指令動態管理，此欄可留空
# TELEGRAM_CHAT_ID=your-chat-id-here

# ── 訂閱者資料目錄 ─────────────────────────────────────────
# Zeabur 請掛載持久化 Volume 至此路徑，確保重啟後訂閱資料不遺失
DATA_DIR=/app/data

# ── 排程設定 ───────────────────────────────────────────────
# 星期幾執行（預設 monday）
# 可選：monday / tuesday / wednesday / thursday / friday / saturday / sunday
SCHEDULE_DAY=monday

# 執行時間，24 小時制 HH:MM（預設 08:00）
SCHEDULE_TIME=08:00

# 時區（預設 Asia/Taipei）
TZ=Asia/Taipei

# ── Email 發送（可選）──────────────────────────────────────
# 若不設定 EMAIL_HOST，Email 功能自動略過，不影響 Telegram 發送

# SMTP 伺服器位址（自架 Mail Server 填內部 IP 或 Domain）
# EMAIL_HOST=mail.company.com

# SMTP 埠號（587 = STARTTLS 推薦；465 = SSL；25 = 無加密）
# EMAIL_PORT=587

# 登入帳號與密碼
# EMAIL_USER=bot@company.com
# EMAIL_PASSWORD=your-smtp-password

# 寄件人顯示名稱（建議格式：顯示名稱 <email>）
# EMAIL_FROM=AI 快報小秘書 <bot@company.com>

# 收件人清單，逗號分隔（全公司同仁）
# EMAIL_RECIPIENTS=all@company.com,team@company.com

# 是否啟用 STARTTLS（預設 true，建議保留）
# EMAIL_USE_TLS=true

# 是否使用 SSL（埠 465 時設 true，預設 false）
# EMAIL_USE_SSL=false

# 郵件主旨前綴（預設「【AI 週報】」）
# EMAIL_SUBJECT_PREFIX=【AI 週報】
